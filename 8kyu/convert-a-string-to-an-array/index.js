// Write a function to split a string and convert it into an array of words

function stringToArray(str) {
    return str.split('')
}

let words = 'i have to go to work soon'

console.log(stringToArray(words))